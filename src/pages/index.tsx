import type { NextPage } from "next";
import { ScreenDescription, SearchInput, TopHeader } from "../components";

const Home: NextPage = () => {
  return (
    <>
      <TopHeader></TopHeader>
    </>
  );
};

export default Home;
